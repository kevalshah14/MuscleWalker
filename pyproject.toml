[project]
name = "musclewalker"
version = "0.1.0"
description = "A Python project for muscle tracking and analysis using MuJoCo"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mujoco[python]>=3.0.0",
    "numpy>=1.24.0",
]
